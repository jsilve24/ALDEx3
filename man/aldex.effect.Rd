% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aldex-summary.R
\name{aldex.effect}
\alias{aldex.effect}
\title{ALDEx3 Effect Diagnostics}
\usage{
aldex.effect(object, contrast)
}
\arguments{
\item{object}{An object returned by \code{aldex}.}

\item{contrast}{The exact model coefficient or unambiguous binary data
column to summarize.}
}
\value{
A data.frame with columns \code{parameter}, \code{entity}, \code{estimate},
\code{pooled.SD}, \code{cohens.d}, and \code{overlap}.
}
\description{
ALDEx2-inspired effect diagnostics for an ALDEx3 binary contrast
}
\details{
\code{aldex.effect} is a helper for effect-size diagnostics that Greg Gloor and
ALDEx2 users commonly inspect when evaluating pairwise effects. It is not a
replacement for \code{summary.aldex}, which reports model estimates, standard
errors and adjusted p-values. This function summarizes a single binary
ALDEx3 model contrast using Cohen's-d-based Monte Carlo diagnostics.

For a selected contrast row \code{k}, feature \code{d}, Monte Carlo draw \code{s}, and
binary group indicators \code{x = object$X[k, ]}, let \code{B[d, s]} be the fitted
ALDEx3 coefficient stored in \code{object$estimate[k, d, s]}. Let
\code{logW[d, i, s] = object$logComp[d, i, s] + object$logScale[i, s]} be the
reconstructed log abundance for sample \code{i}. \code{aldex.effect} requires
\code{logComp} and \code{logScale}, so it is unavailable when \code{aldex} streams large
jobs and omits those arrays. For each feature and Monte Carlo draw, the
pooled within-group variance is
\code{((n0 - 1) * var(logW[d, x == 0, s]) + (n1 - 1) * var(logW[d, x == 1, s])) / (n0 + n1 - 2)}, where \code{n0} and \code{n1} are the
group sizes.

The returned columns are calculated as follows:
\itemize{
\item \code{estimate}: the mean of \code{B[d, s]} over Monte Carlo draws.
\item \code{pooled.SD}: the mean of the pooled within-group standard deviation,
\verb{sqrt(pooled variance)}, over Monte Carlo draws.
\item \code{cohens.d}: the mean of \code{B[d, s] / pooled.SD[d, s]} over Monte Carlo
draws. This is Cohen's-d-based and is not the same as the historical
robust ALDEx2 effect statistic based on between/within dispersion.
\item \code{overlap}: an ALDEx2-style directional uncertainty diagnostic. It counts
the Monte Carlo Cohen's d draws below and above zero, smooths those two
counts with a 0.5 pseudocount, converts them with the ALDEx/Aitchison
mean, and returns the smaller smoothed sign probability. Values near 0
indicate that nearly all Monte Carlo effect draws have the same sign.
Values near 0.5 indicate uncertainty about direction. \code{overlap} is not a
p-value, a confidence interval probability, or a literal area of overlap
between two density curves.
}
}
\author{
Greg Gloor, Justin Silverman
}
